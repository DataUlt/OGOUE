// Topbar Actions
window.OGOUE = window.OGOUE || {};

window.OGOUE.topbarActions = {
    init: function() {
        // Setup topbar button actions if needed
    }
};

document.addEventListener('DOMContentLoaded', function() {
    window.OGOUE.topbarActions.init();
});
